apiVersion: v1
kind: Service
metadata:
  name: {{ include "plot-reel.fullname" . }}
  labels:
    {{- include "plot-reel.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "plot-reel.selectorLabels" . | nindent 4 }}